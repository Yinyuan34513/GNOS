# ---- build outputs ----
# build/ holds the compiled kernel/initrd, the bootable ISO, and the
# hand-fetched third-party toolchain trees (musl, busybox). It is fully
# regenerated by `make`, so nothing under it belongs in version control.
/build/

# ---- editor / OS noise ----
*.swp
*.swo
.DS_Store
*~
